set(PACKAGE_VERSION "@mediocr-dicom_VERSION_MAJOR@.@mediocr-dicom_VERSION_MINOR@")
set(PACKAGE_VERSION_EXACT FALSE)
set(PACKAGE_VERSION_COMPATIBLE FALSE)
set(PACKAGE_VERSION_UNSUITABLE TRUE)

if(${PACKAGE_FIND_NAME} STREQUAL "mediocr-dicom")
	if(${PACKAGE_FIND_VERSION_MAJOR} EQUAL @mediocr-dicom_VERSION_MAJOR@)
		if(${PACKAGE_FIND_VERSION_MINOR} EQUAL @mediocr-dicom_VERSION_MINOR@)
			set(PACKAGE_VERSION_EXACT TRUE)
			set(PACKAGE_VERSION_COMPATBLE TRUE)
			set(PACKAGE_VERSION_UNSUITABLE FALSE)
		elseif(${PACKAGE_FIND_VERSION_MINOR} LESS @mediocr-dicom_VERSION_MINOR@)
			set(PACKAGE_VERSION_COMPATIBLE TRUE)
			set(PACKAGE_VERSION_UNSUITABLE FALSE)
		endif()
	endif()
endif()
